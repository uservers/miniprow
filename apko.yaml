contents:
  keyring:
      - https://packages.wolfi.dev/os/wolfi-signing.rsa.pub
  repositories:
    - https://packages.wolfi.dev/os
    - '@local packages'
  packages:
    - wolfi-base
    - libstdc++
    - git
    - miniprow@local
  
entrypoint:
  command: /usr/bin/broker
